""" Create a list """

list1 = [10, 20, 15, 12, 200] # assign and initialise the array
print(type(list1)) # returns the list type class
print(list)

""" Convert the list into bytes """
byteVal = bytes(list1)
print(type(byteVal))
print(byteVal)

""" Convert the list into bytes Array """
byteArrayVal = bytearray(list1)
print(type(byteArrayVal))
print(byteArrayVal)
